syntax = "proto3";

package machina.serving.test_util;

// Config proto for FakeLoaderSourceAdapter.
message FakeLoaderSourceAdapterConfig {
  // FakeLoaderSourceAdapter's 'suffix' ctor parameter.
  string suffix = 1;
}
